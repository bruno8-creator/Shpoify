<section class="page-template">
  <div class="page-width">
    <div class="section-heading">
      <h1>{{ page.title }}</h1>
    </div>
    <div class="rte">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Página principal"
}
{% endschema %}
